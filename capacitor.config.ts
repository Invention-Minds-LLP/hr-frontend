import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hrminds.in',
  appName: 'HR Minds',
  webDir: 'dist/hr-frontend/browser'
};

export default config;
